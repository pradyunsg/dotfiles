# CONFIG:: Set the prompt
PROMPT_THEME=powerline-basic

if [ -z "$DO_NOT_SET_PROMPT" ]; then
    # Source the prompt
    echo "Activating PROMPT: ${PROMPT_THEME}"
    source ~/.sh-common/prompt/${PROMPT_THEME}/prompt.sh
fi
