# asdf
.tool-versions

# Visual Studio Code
.vscode
.devcontainer

# Ignore the file that specifies what virtualenv is to be used locally
.venv
.venvname

# Makes it easier to ignore files that I don't want to commit by mistake
*.ignore
*.ignore.*

# Avoid accidentally committing an empty blame ignore file
.git-blame-ignore-revs

# Generated files
/build/
*.[oa]
*.pyc
*.egg-info/

# Mac OS X
*.DS_Store

# Because I hate these
*.fuse_hidden*
.*cache
