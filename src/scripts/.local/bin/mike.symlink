#!/usr/bin/env python3

import subprocess
import sys


def main():
    # Show help, by default.
    make_args = list(sys.argv[1:] or ["help"])
    subprocess.run(["make"] + make_args)


if __name__ == "__main__":
    main()
