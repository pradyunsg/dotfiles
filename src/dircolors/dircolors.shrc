[[ "$OSTYPE" == darwin* ]] && return

_pradyunsg_log "Setting up dircolors..."

command -v dircolors >/dev/null 2>&1 || return

if [ -z $DIRCOLORS_FILE ]; then
    DIRCOLORS_FILE=${DOTFILES_LOCATION}/dircolors/default.dircolors
fi
eval $(dircolors $DIRCOLORS_FILE)
