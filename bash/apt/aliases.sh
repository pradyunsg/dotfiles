#!/bin/bash

# Typing sudo apt-get... is a pain in the a**
alias apt-ug="sudo apt upgrade"
alias apt-ugf="sudo apt full-upgrade"

alias apt-ud="echo 'Updating cache...' && sudo apt-get update > /dev/null"
